name: Build and deploy dashboard

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  # run at 00:00, 06:00, 12:00 and 18:00 every day
  schedule:
    - cron: '0 0,6,12,18 * * *'
  workflow_dispatch:

env:
  PAGE_URL: https://canonical.github.io/chisel-releases-navigator/

jobs:
  build:
    name: build
    runs-on: ubuntu-24.04
    permissions:
      actions: read
      contents: read
    steps:
      - name: Checkout Dashboard Repository
        uses: actions/checkout@v6

      # TODO: reenable caching of the image

      # - name: Restore The Dev Image
      #   id: cache
      #   uses: actions/cache/restore@v5
      #   with:
      #     key: dev-image-${{ hashFiles('Dockerfile') }}
      #     path: dev-image.tar

      - name: Install uv
        uses: astral-sh/setup-uv@v7
      
      - name: Install distro-info
        run: |
          sudo apt-get update
          sudo apt-get install --reinstall --yes distro-info distro-info-data

      # https://github.com/actions/checkout/tree/v6/?tab=readme-ov-file#fetch-only-a-single-file
      - name: Checkout the chisel-releases-data action
        uses: actions/checkout@v6
        with:
          repository: canonical/chisel-releases-data
          path: actions/chisel-releases-data
          token: ${{ secrets.CHISEL_RELEASES_DATA_TOKEN }}
          sparse-checkout: action.yaml
          sparse-checkout-cone-mode: false

      - name: Download latest index.db.br
        uses: ./actions/chisel-releases-data
        with:
          artifact_name: index.db.br
          output_path: ./data_scraper
          token: ${{ secrets.CHISEL_RELEASES_DATA_TOKEN }}

      - name: Verify index.db.br download
        run: ls -la ./data_scraper/index.db.br

      - name: Build Dashboard
        # env:
        #   GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: make build-dist
      
      # - name: Cache The Dev Image
      #   if: steps.cache.outputs.cache-hit != 'true'
      #   uses: actions/cache@v5
      #   with:
      #     key: dev-image-${{ hashFiles('Dockerfile') }}
      #     path: dev-image.tar
    
      - name: Upload Artifact
        uses: actions/upload-artifact@v6
        with:
          name: dashboard-dist
          path: dashboard/dist

  deploy:
    name: deploy
    needs: build
    if: github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') || github.event_name == 'schedule'
    permissions:
      contents: read
      pages: write
      id-token: write
    concurrency:
      group: "pages"
      cancel-in-progress: false
    environment:
      name: github-pages
      url: ${{ env.PAGE_URL }}
    runs-on: ubuntu-latest
    steps:
      - name: Download Artifact
        uses: actions/download-artifact@v7
        with:
          name: dashboard-dist
      - name: Debug ls
        run: ls 
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: .
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        